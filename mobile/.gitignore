# Expo / React Native
node_modules/
.expo/
dist/
web-build/
expo-env.d.ts

# Native projects — regenerated by `expo prebuild` / EAS cloud build (managed workflow)
/android
/ios

# Native
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# Debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store

# Build outputs
*.apk
*.aab
*.zip

# Env (keep committed .env for the EXPO_NO_METRO_WORKSPACE_ROOT build flag)
.env*.local

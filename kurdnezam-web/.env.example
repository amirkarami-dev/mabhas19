# Base URL of the kurdnezam backend API.
# Read at BUILD time (NEXT_PUBLIC_*) — it is baked into the client bundle and
# into next.config.ts's images.remotePatterns, so a change needs a rebuild.
#
#   dev  : http://localhost:5000
#   prod : https://api.mabhas19.myceo.ir
NEXT_PUBLIC_API_BASE=http://localhost:5000
